
import background from '../assets/temporary-background.gif'

function Background() {
    return (
        <div style={{backgroundImage: `url(${background})` }}>
             test
        </div>
    );
}

export default Background
